# C4 Diagrams — Cross-Language Conversation Platform

Three levels, each answering a different question. Rendered natively
by GitHub; source is plain Mermaid, editable as text.

## Level 1 — System Context

*What sits around this platform, and where does the boundary fall?*

```mermaid
C4Context
    title System Context — Cross-Language Conversation Platform

    Person(participant, "Participant", "Speaks in their own language via the calling application")

    System_Ext(callingApp, "Calling Application", "The developer's own meeting / call / voice product. Owns microphone access, devices, participant UI, and transport. Bring-your-own-transport.")

    System(platform, "Cross-Language Conversation Platform", "Composes one-way translation sessions into conversation use cases. Provider-agnostic. Never touches a device.")

    System_Ext(provider, "Translation Provider", "OpenAI Realtime Translation API today; Glot's own API is a swappable connector")

    Rel(participant, callingApp, "Speaks / listens")
    Rel(callingApp, platform, "Streams audio in, receives translated audio + transcript", "WebSocket")
    Rel(platform, provider, "One session per source→target direction", "WebSocket")

    UpdateLayoutConfig($c4ShapeInRow="2", $c4BoundaryInRow="1")
```

## Level 2 — Container

*What is actually built, and what is described but deliberately not built?*

```mermaid
C4Container
    title Container — Cross-Language Conversation Platform

    System_Ext(callingApp, "Calling Application", "Bring-your-own-transport")

    Container_Boundary(platform, "Cross-Language Conversation Platform") {
        Container(api, "API Layer", "FastAPI", "DESCRIBED, NOT BUILT. POST /v1/conversations, POST /v1/broadcasts, WS /v1/conversations/{id}/stream. Routes participant_id-tagged audio to the right use case.")
        Container(translation, "translation package", "Python (asyncio)", "BUILT + TESTED. The abstraction the brief evaluates: session primitive, use-case composition, provider boundary.")
    }

    System_Ext(provider, "Translation Provider", "OpenAI Realtime today; Glot connector next")

    Rel(callingApp, api, "HTTP + WebSocket")
    Rel(api, translation, "In-process calls")
    Rel(translation, provider, "One-way sessions", "WebSocket")

    UpdateLayoutConfig($c4ShapeInRow="2", $c4BoundaryInRow="1")
```

## Level 3 — Component (`translation` package)

*Module boundaries and provider-agnosticism, made visible. The arrows
are the argument: `Conversation` and `Broadcast` point only at the
abstract interface, never at a concrete provider.*

```mermaid
C4Component
    title Component — translation package

    Container_Boundary(pkg, "translation package") {
        Component(session, "TranslationSession", "abstract base class", "One-way, one language pair. start / send_audio / events / close. The single primitive everything composes.")

        Component(conversation, "Conversation", "orchestration", "Two participants → two sessions, one per direction. Takes a session_factory; never imports a provider.")
        Component(broadcast, "Broadcast", "orchestration", "One speaker → N target languages, one session each. Same session_factory. Proves the abstraction generalises.")
        Component(registry, "ConversationRegistry", "in-memory", "Tracks currently-live Conversations by id. Create / get / end. Not persistence.")

        Component(fake, "FakeTranslationSession", "provider", "No network. Echoes UTF-8 input prefixed with target language. Drives every test and the demo.")
        Component(openai, "OpenAIRealtimeSession", "provider", "Real async WebSocket client. Exercised end-to-end against the live API.")
    }

    System_Ext(provider, "OpenAI Realtime Translation API", "External")

    Rel(conversation, session, "depends on (abstract only)")
    Rel(broadcast, session, "depends on (abstract only)")
    Rel(registry, conversation, "creates / tracks / ends")
    Rel(fake, session, "implements")
    Rel(openai, session, "implements")
    Rel(openai, provider, "wss://", "WebSocket")

    UpdateLayoutConfig($c4ShapeInRow="3", $c4BoundaryInRow="1")
```

## Reading the component diagram

The provider boundary is the whole point. Both use cases (`Conversation`,
`Broadcast`) have exactly one dependency arrow, and it lands on the
abstract `TranslationSession` — not on `FakeTranslationSession`, not on
`OpenAIRealtimeSession`. Swapping providers means passing a different
factory; no orchestration code changes. A third provider
(`GlotSession`) would appear as one more box on the right, with one
more "implements" arrow, and nothing else in the diagram would move.
